name = "job-flow"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
# Non-sensitive config goes here

# R2 bucket for resume storage and job outputs
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "job-flow"

# Secrets (set via `wrangler secret put`):
# - ANTHROPIC_API_KEY
# - API_TOKEN (for authenticating requests to this worker)
